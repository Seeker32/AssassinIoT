// Code generated by ent, DO NOT EDIT.

package ent

import (
	"AssassinIoT/ent/modelcategory"
	"AssassinIoT/ent/tenant"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ModelCategory is the model entity for the ModelCategory schema.
type ModelCategory struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 所属租户
	TenantKey string `json:"tenant_key,omitempty"`
	// 分类标识，如 temp_humidity_sensor、smart_lock
	CategoryKey string `json:"category_key,omitempty"`
	// 分类展示名称，如'温湿度传感器'、'智能门锁'
	DisplayName string `json:"display_name,omitempty"`
	// 描述信息
	Description string `json:"description,omitempty"`
	// 图标标识
	Icon string `json:"icon,omitempty"`
	// 排序值，数字越小越靠前
	SortOrder int `json:"sort_order,omitempty"`
	// 租户 ID 外键
	TenantID int `json:"tenant_id,omitempty"`
	// 状态：active=启用, disabled=禁用
	Status modelcategory.Status `json:"status,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ModelCategoryQuery when eager-loading is set.
	Edges        ModelCategoryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ModelCategoryEdges holds the relations/edges for other nodes in the graph.
type ModelCategoryEdges struct {
	// Tenant holds the value of the tenant edge.
	Tenant *Tenant `json:"tenant,omitempty"`
	// ThingModels holds the value of the thing_models edge.
	ThingModels []*ThingModel `json:"thing_models,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// TenantOrErr returns the Tenant value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ModelCategoryEdges) TenantOrErr() (*Tenant, error) {
	if e.Tenant != nil {
		return e.Tenant, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: tenant.Label}
	}
	return nil, &NotLoadedError{edge: "tenant"}
}

// ThingModelsOrErr returns the ThingModels value or an error if the edge
// was not loaded in eager-loading.
func (e ModelCategoryEdges) ThingModelsOrErr() ([]*ThingModel, error) {
	if e.loadedTypes[1] {
		return e.ThingModels, nil
	}
	return nil, &NotLoadedError{edge: "thing_models"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ModelCategory) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case modelcategory.FieldID, modelcategory.FieldSortOrder, modelcategory.FieldTenantID:
			values[i] = new(sql.NullInt64)
		case modelcategory.FieldTenantKey, modelcategory.FieldCategoryKey, modelcategory.FieldDisplayName, modelcategory.FieldDescription, modelcategory.FieldIcon, modelcategory.FieldStatus:
			values[i] = new(sql.NullString)
		case modelcategory.FieldCreatedAt, modelcategory.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ModelCategory fields.
func (_m *ModelCategory) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case modelcategory.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case modelcategory.FieldTenantKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_key", values[i])
			} else if value.Valid {
				_m.TenantKey = value.String
			}
		case modelcategory.FieldCategoryKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field category_key", values[i])
			} else if value.Valid {
				_m.CategoryKey = value.String
			}
		case modelcategory.FieldDisplayName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field display_name", values[i])
			} else if value.Valid {
				_m.DisplayName = value.String
			}
		case modelcategory.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		case modelcategory.FieldIcon:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field icon", values[i])
			} else if value.Valid {
				_m.Icon = value.String
			}
		case modelcategory.FieldSortOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sort_order", values[i])
			} else if value.Valid {
				_m.SortOrder = int(value.Int64)
			}
		case modelcategory.FieldTenantID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value.Valid {
				_m.TenantID = int(value.Int64)
			}
		case modelcategory.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = modelcategory.Status(value.String)
			}
		case modelcategory.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case modelcategory.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ModelCategory.
// This includes values selected through modifiers, order, etc.
func (_m *ModelCategory) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTenant queries the "tenant" edge of the ModelCategory entity.
func (_m *ModelCategory) QueryTenant() *TenantQuery {
	return NewModelCategoryClient(_m.config).QueryTenant(_m)
}

// QueryThingModels queries the "thing_models" edge of the ModelCategory entity.
func (_m *ModelCategory) QueryThingModels() *ThingModelQuery {
	return NewModelCategoryClient(_m.config).QueryThingModels(_m)
}

// Update returns a builder for updating this ModelCategory.
// Note that you need to call ModelCategory.Unwrap() before calling this method if this ModelCategory
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ModelCategory) Update() *ModelCategoryUpdateOne {
	return NewModelCategoryClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ModelCategory entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ModelCategory) Unwrap() *ModelCategory {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ModelCategory is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ModelCategory) String() string {
	var builder strings.Builder
	builder.WriteString("ModelCategory(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("tenant_key=")
	builder.WriteString(_m.TenantKey)
	builder.WriteString(", ")
	builder.WriteString("category_key=")
	builder.WriteString(_m.CategoryKey)
	builder.WriteString(", ")
	builder.WriteString("display_name=")
	builder.WriteString(_m.DisplayName)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteString(", ")
	builder.WriteString("icon=")
	builder.WriteString(_m.Icon)
	builder.WriteString(", ")
	builder.WriteString("sort_order=")
	builder.WriteString(fmt.Sprintf("%v", _m.SortOrder))
	builder.WriteString(", ")
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TenantID))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ModelCategories is a parsable slice of ModelCategory.
type ModelCategories []*ModelCategory
